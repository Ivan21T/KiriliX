функция цяло събери(цяло а, цяло б) {
    върни а + б;
}

функция цяло факториел(цяло н) {
    ако (н <= 1) {
        върни 1;
    }
    върни н * факториел(н - 1);
}

функция празно главна() {
    печатай(събери(5, 7));
    печатай(факториел(5));
    върни нищо;
}
